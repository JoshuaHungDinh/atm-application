namespace Atm.UnitTests;

/// <summary>
/// Placeholder so the test project builds and CI has a green run to gate on.
/// Real domain and application tests replace this in PR 2.
/// </summary>
public class ScaffoldingTests
{
    [Fact]
    public void Test_project_is_wired_and_runner_executes()
    {
        Assert.Equal(4, 2 + 2);
    }
}
