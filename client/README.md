# ATM — client

React + TypeScript SPA (Vite). See the repository root `README.md` for how to run it
alongside the API.

```bash
npm install
npm run dev     # http://localhost:5173, proxies /accounts and /transfers to :5078
npm run build   # production bundle in dist/
```
